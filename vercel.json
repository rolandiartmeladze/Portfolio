{
    "version": 2,
    "builds": [
      {
        "src": "Backend/app.py",
        "use": "@vercel/python"
      },
      {
        "src": "src/**/*.tsx",
        "use": "@vercel/static-build",
        "config": {
          "distDir": "build"
        }
      }
    ],
    "routes": [
      {
        "src": "/api/(.*)",
        "dest": "/api/$1"
      },
      {
        "src": "/(.*)",
        "dest": "/index.html"
      }
    ]
  }
  